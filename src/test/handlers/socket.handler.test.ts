// import { socket } from '../../socket/socket.handler'

// let socketCalls: { e: string; cb: () => void }[]

// beforeEach(() => {
//   ;(socket.on as any) = jest.fn()
// })

// describe('Socket tests', () => {
//   test('does x', () => {
//     socket.on('x', () => {})
//     console.log(socketCalls)
//   })
// })

test('x', () => {})
